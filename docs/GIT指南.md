# Git 协作完整流程（图解版）

## 📖 目录
1. [初始化设置](#1-初始化设置)
2. [完整工作流程](#2-完整工作流程)
3. [实际案例演示](#3-实际案例演示)
4. [常见问题排查](#4-常见问题排查)
5. [快速参考](#5-快速参考)

---

## 1. 初始化设置

### 1.1 苏崇博（负责人）操作

```bash
# ===== 第一步：初始化并推送到远程 =====
cd Housing-Factor-Mining
git add .
git commit -m "chore: 初始化项目框架"
git push origin main

# ===== 第二步：创建dev分支 =====
git checkout -b dev
git push -u origin dev

# ===== 第三步：在GitHub/Gitee添加组员为协作者 =====
# 设置 -> 协作者 -> 添加组员的GitHub用户名
```

### 1.2 其他组员第一次克隆

```bash
# ===== 步骤1：克隆仓库 =====
git clone https://github.com/Sandstorm-Bo/Housing-Factor-Mining.git
cd Housing-Factor-Mining

# ===== 步骤2：查看所有分支 =====
git branch -a
# 输出：
#   * main
#     remotes/origin/main
#     remotes/origin/dev

# ===== 步骤3：切换到dev分支 =====
git checkout dev

# ===== 步骤4：创建自己的分支,直接用姓名 =====
# 王俊涛：
git checkout -b feature/wangjt

# 侯钧译：
git checkout -b feature/houjy

# 王梓玥：
git checkout -b feature/wangzy

# 郝竞妍：
git checkout -b feature/haojy

# ===== 步骤5：推送自己的分支到远程 =====
git push -u origin feature/你的分支名
```

---

## 2. 完整工作流程

### 流程图

```
┌─────────────────────────────────────────────────────┐
│  开始工作前：同步dev分支最新代码                      │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  在自己的feature分支上开发                           │
│  - 编写代码                                          │
│  - 定期 git add + commit                            │
│  - 定期 git push 到远程                             │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  功能完成：创建Pull Request                          │
│  - 在GitHub/Gitee上提交PR                           │
│  - 等待代码审查                                      │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  苏崇博审查并合并到dev                               │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│  继续下一个功能（回到第一步）                        │
└─────────────────────────────────────────────────────┘
```

### 详细步骤

#### 第1步：开始工作前（每次都要做！）

```bash
# 1. 查看当前分支
git branch
# 输出：* feature/你的分支名

# 2. 切换到dev分支
git checkout dev

# 3. 拉取最新代码
git pull origin dev

# 4. 回到自己的分支
git checkout feature/你的分支名

# 5. 合并dev的最新代码到自己分支
git merge dev
```

**为什么要这样做？**
确保你的代码基于最新的dev分支，避免冲突越积越多。

---

#### 第2步：开发中

```bash
# ===== 场景1：完成了一个小功能 =====
git add .
git commit -m "feat: 完成链家爬虫登录功能"
git push origin feature/你的分支名

# ===== 场景2：只修改了某个文件 =====
git add backend/crawler/spider.py
git commit -m "fix: 修复爬虫超时问题"
git push origin feature/你的分支名

# ===== 场景3：查看当前修改了什么 =====
git status  # 查看哪些文件被修改
git diff    # 查看具体修改内容
```

**建议**：
- 每完成一个小功能就commit一次
- 每天至少push一次到远程
- commit信息要清晰描述做了什么

---

#### 第3步：提交Pull Request

```bash
# 1. 最后一次同步dev分支
git checkout dev
git pull origin dev
git checkout feature/你的分支名
git merge dev

# 2. 解决冲突（如果有）
# 见下文"冲突解决详解"

# 3. 推送到远程
git push origin feature/你的分支名

# 4. 在GitHub/Gitee上创建PR
```

**在GitHub/Gitee网页上操作**：
1. 进入仓库页面
2. 点击 "Pull requests" -> "New pull request"
3. Base: `dev` ← Compare: `feature/你的分支名`
4. 填写标题：`[爬虫] 完成链家数据爬取功能`
5. 填写描述：
   ```
   ## 完成内容
   - 实现链家爬虫框架
   - 支持海淀、朝阳等5个区域
   - 数据存储到backend/data/raw/

   ## 测试
   - 已测试爬取2023年1月数据
   - 无报错

   ## 注意事项
   - 需要安装requests库
   ```
6. 点击 "Create pull request"
7. 通知scb即时审查!

---

#### 第4步：代码审查（苏崇博操作）

```bash
# 1. 查看PR内容
# 在GitHub/Gitee网页上查看代码变更

# 2. 本地测试（如果需要）
git fetch origin
git checkout feature/组员的分支名
# 运行代码测试

# 3. 审查通过，合并到dev
# 在网页上点击"Merge pull request"

# 4. 删除已合并的分支（可选）
git push origin --delete feature/组员的分支名
```

---

## 3. 实际案例演示

### 案例1：开发爬虫功能

```bash
# ===== Day 1: 开始开发 =====
cd Housing-Factor-Mining
git checkout dev
git pull origin dev
git checkout feature/crawler-lianjia

# 创建爬虫文件
# 编写代码...

git add backend/crawler/lianjia.py
git commit -m "feat: 创建链家爬虫基础框架"
git push origin feature/crawler-lianjia

# ===== Day 2: 继续开发 =====
git checkout dev
git pull origin dev  # 拉取别人的更新
git checkout feature/crawler-lianjia
git merge dev  # 合并到自己分支

# 继续编写代码...

git add backend/crawler/lianjia.py
git commit -m "feat: 添加数据解析功能"
git push origin feature/crawler-lianjia

# ===== Day 3: 功能完成 =====
# 同步dev
git checkout dev
git pull origin dev
git checkout feature/crawler-lianjia
git merge dev

# 推送并创建PR
git push origin feature/crawler-lianjia
# 在GitHub上创建Pull Request

# ===== 审查通过后，开始新功能 =====
git checkout dev
git pull origin dev  # 拉取已合并的代码
git checkout -b feature/crawler-anjuke  # 创建新分支
git push -u origin feature/crawler-anjuke
```

---

### 案例2：王俊涛的前端开发

```bash
# ===== 初始化前端项目 =====
git checkout dev
git pull origin dev
git checkout -b feature/frontend-vue

# 在frontend/目录初始化Vue项目
npm create vite@latest .
npm install

git add frontend/
git commit -m "feat: 初始化Vue项目"
git push -u origin feature/frontend-vue

# ===== 开发第一个组件 =====
# 创建 PriceTrendChart.vue...

git add frontend/src/components/PriceTrendChart.vue
git commit -m "feat: 完成价格趋势图表组件"
git push origin feature/frontend-vue

# ===== 第二天继续开发 =====
# 同步dev（重要！）
git checkout dev
git pull origin dev
git checkout feature/frontend-vue
git merge dev

# 如果出现冲突
# 手动解决冲突后...
git add .
git commit -m "fix: 解决合并冲突"

# 继续开发...
```

---

## 4. 常见问题排查

### Q1: 忘记切换分支，在dev上直接开发了

```bash
# 情况：你在dev分支修改了代码，但还没commit

# 解决方法：
# 1. 保存当前修改
git stash

# 2. 切换到正确的分支
git checkout feature/你的分支名

# 3. 恢复修改
git stash pop

# 4. 提交
git add .
git commit -m "feat: 你的功能"
```

---

### Q2: 合并时出现冲突

```bash
# 执行merge时出现：
# CONFLICT (content): Merge conflict in backend/crawler/spider.py
# Automatic merge failed; fix conflicts and then commit the result.

# 步骤1：查看冲突文件
git status
# 输出：both modified: backend/crawler/spider.py

# 步骤2：打开文件，找到冲突标记
"""
def get_data():
    return "我的实现"
def get_data():
    return "别人的实现"
"""

# 步骤3：手动决定保留哪个
"""
def get_data():
    return "保留这个实现"
"""

# 步骤4：标记为已解决
git add backend/crawler/spider.py
git commit -m "fix: 解决merge冲突"
git push origin feature/你的分支名

# 如果实在不会解决，找苏崇博帮忙
```

---

### Q3: 误commit了错误代码

```bash
# 情况1：还没push，只是commit了
git reset --soft HEAD~1  # 撤销commit，保留修改
# 重新修改后再commit

# 情况2：已经push了
git revert HEAD  # 创建一个新commit撤销上次修改
git push origin feature/你的分支名
```

---

### Q4: 想放弃所有本地修改

```bash
# ⚠️ 警告：会丢失所有未提交的修改

# 方法1：放弃所有修改
git checkout .

# 方法2：完全重置到远程状态
git fetch origin
git reset --hard origin/feature/你的分支名
```

---

### Q5: 如何查看别人的代码

```bash
# 查看远程有哪些分支
git branch -a

# 切换到别人的分支
git checkout feature/别人的分支名

# 查看后切回自己的分支
git checkout feature/你的分支名
```

---

## 5. 快速参考

### 每天开始工作前

```bash
git checkout dev && git pull origin dev && git checkout - && git merge dev
```

### 提交代码

```bash
git add . && git commit -m "feat: 你的描述" && git push
```

### 查看状态

```bash
git status  # 查看修改
git log --oneline  # 查看提交历史
git branch  # 查看当前分支
```

### 紧急求助

```bash
# 我在哪个分支？
git branch

# 我修改了什么？
git status
git diff

# 我怎么回到上一次提交的状态？
git checkout .

# 我想撤销上一次commit？
git reset --soft HEAD~1
```



## ⚠️ 重要提醒

### 必须遵守的规则：
1. ✅ **每天开始前先pull dev**
2. ✅ **只在自己的feature分支上开发**
3. ✅ **不要直接push到main或dev**
4. ✅ **通过PR合并代码**

### 禁止操作：
1. ❌ 不要 `git push -f`（强制推送）
2. ❌ 不要在dev分支直接开发
3. ❌ 不要删除别人的分支
4. ❌ 不要修改.gitignore（除非确认需要）

---

## 📞 遇到问题怎么办？

1. **查看本文档的"常见问题"**
2. **在微信群询问**
3. **找苏崇博帮忙**
4. **Git命令不确定时先用`git status`查看状态**

---


